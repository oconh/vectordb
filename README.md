# vectordb
A vector database built from scratch in Python. Implements HNSW (Hierarchical Navigable Small World) indexing for approximate nearest neighbour search.  Built to understand how vector databases actually work under the hood, rather than using an existing library.
